# These enums are unavailable on tvOS natively (API_UNAVAILABLE on the typedef), but the managed enums are bound and used by bound API.
!unknown-native-enum! CSSuggestionKind bound
!unknown-native-enum! CSUserInteraction bound
